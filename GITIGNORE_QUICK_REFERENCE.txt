# .gitignore Quick Reference

## ✅ What's Being Ignored (Now Safe to Push)

### 🔐 SECRETS & ENVIRONMENT (CRITICAL!)
- `.env` files with any sensitive data
- Private keys: `*.pem`, `*.key`, `*.keystore`, `*.p12`
- Credentials: `credentials.json`, `*-credentials.json`
- Service accounts: `service-account-key.json`
- AWS config: `aws-credentials`
- Firebase config: `.firebase-credentials`
- npm token: `.npmrc`

### 📦 DEPENDENCIES
- `node_modules/` (auto-generated by pnpm install)
- `pnpm-lock.yaml` (auto-generated)
- `npm-shrinkwrap.json`
- `yarn.lock`

### 🏗️ BUILD OUTPUTS
- `.next/` (Next.js cache)
- `dist/`, `build/`, `out/` (build outputs)
- `.turbo/` (Turbo cache)
- `coverage/` (test coverage)

### 💻 IDE SETTINGS (Personal)
- `.vscode/settings.json`
- `.vscode/launch.json`
- `.idea/` (JetBrains)

### 🖥️ OS FILES
- `.DS_Store` (macOS)
- `Thumbs.db` (Windows)

### 📋 LOGS
- `*.log` (all log files)
- `npm-debug.log*`

---

## 📤 BEFORE YOU PUSH

```bash
# 1. Check what will be pushed
git status

# 2. Should see ONLY:
#    - src/
#    - packages/
#    - README.md
#    - etc.
#
# Should NOT see:
#    - .env
#    - node_modules/
#    - .next/
#    - .vscode/settings.json

# 3. Verify no secrets in staging
git diff --cached | grep -i "secret\|password\|key\|token"
# Should return: nothing

# 4. If all good, push!
git push
```

---

## ⚠️ CRITICAL - NEVER COMMIT

```
❌ .env files
❌ Private keys
❌ API keys / tokens
❌ Passwords
❌ Database credentials
❌ AWS / Google Cloud keys
❌ JWT secrets
```

If you see these in `git status`, **DO NOT COMMIT!**

---

## 📋 SUMMARY: 11 Protected Categories

| Category | Examples | Status |
|----------|----------|--------|
| Secrets | `.env`, `*.key`, `credentials.json` | ✅ Ignored |
| Dependencies | `node_modules/`, `pnpm-lock.yaml` | ✅ Ignored |
| Build | `.next/`, `dist/`, `build/` | ✅ Ignored |
| IDE | `.vscode/settings.json`, `.idea/` | ✅ Ignored |
| OS | `.DS_Store`, `Thumbs.db` | ✅ Ignored |
| Logs | `*.log`, `npm-debug.log*` | ✅ Ignored |
| Testing | `coverage/`, `test-results/` | ✅ Ignored |
| Cache | `.cache/`, `.turbo/`, `.eslintcache` | ✅ Ignored |
| Temp | `tmp/`, `*.tmp`, `.cache/` | ✅ Ignored |
| Database | `*.db`, `*.sqlite`, `*.sql` | ✅ Ignored |
| TypeScript | `next-env.d.ts`, `*.tsbuildinfo` | ✅ Ignored |

**Your project is secure!** ✅
