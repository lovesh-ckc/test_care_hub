// import { LayoutPresets } from "@infinity/layout-engine";

// function prefixClasses(prefix: string, classes: string) {
//   return classes
//     .split(" ")
//     .filter(Boolean)
//     .map((cls) => `${prefix}${cls}`)
//     .join(" ");
// }

// export const responsiveContainer = [
//   LayoutPresets.container.mobile,
//   prefixClasses("sm:", LayoutPresets.container.tablet),
//   prefixClasses("lg:", LayoutPresets.container.desktop),
// ].join(" ");
